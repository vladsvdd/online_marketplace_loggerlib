# Автоматическое и ручное версионирование

Этот репозиторий содержит workflows для автоматического и ручного управления версиями.

## Доступные Workflows

### `auto-version.yml` - Автоматическое версионирование
- Запускается при каждом push в ветку `master`
- Автоматически увеличивает patch версию при изменениях кода
- Игнорирует изменения только в документации

### `tag-latest.yml` - Ручное версионирование
- Запускается вручную через GitHub Actions
- Позволяет выбрать тип версии (major/minor/patch)
- Для создания релизов и специальных версий

## Автоматическое версионирование

### Триггер
- Запускается при каждом `push` в ветку `master`
- Игнорирует изменения только в документации (`.md`, `README`, `.gitignore`)

### Логика
1. **Проверка изменений**: Анализирует, есть ли изменения в коде
2. **Проверка флага**: Ищет `[skip version]` в сообщении коммита
3. **Версионирование**: Если нужно, увеличивает patch версию (1.0.0 → 1.0.1)

## Использование автоматического версионирования

**Примечание**: Автоматическое версионирование всегда использует `patch` версию (1.0.0 → 1.0.1).

### Обычный коммит (создаст новую patch версию)
```bash
git add .
git commit -m "feat: add new logging functionality"
git push origin master
# Результат: v1.0.0 → v1.0.1
```

### Коммит без версионирования
```bash
git commit -m "docs: update readme [skip version]"
git push origin master
# Результат: версия не изменится
```

### Изменения только в документации (версия не изменится)
```bash
git commit -m "docs: update installation guide"
git push origin master
# Результат: версия не изменится (только .md файлы)
```

### Примеры автоматического patch версионирования:
```bash
# Исправление бага
git commit -m "fix: resolve memory leak in logger"
git push origin master
# v1.0.0 → v1.0.1

# Новая функция
git commit -m "feat: add OAuth2 authentication"
git push origin master
# v1.0.1 → v1.0.2

# Рефакторинг
git commit -m "refactor: improve error handling"
git push origin master
# v1.0.2 → v1.0.3
```

## Ручное версионирование

Если вам нужно создать версию вручную (например, для релиза), используйте workflow `tag-latest.yml`:

### Как запустить:
1. Перейдите в GitHub репозиторий
2. Откройте вкладку **Actions**
3. Выберите workflow **"Tag Latest Version"**
4. Нажмите **"Run workflow"**
5. Выберите тип версии:
   - **patch** - исправления (1.0.0 → 1.0.1)
   - **minor** - новые функции (1.0.0 → 1.1.0)
   - **major** - breaking changes (1.0.0 → 2.0.0)
6. Нажмите **"Run workflow"**

### Когда использовать ручное версионирование:
- Создание релизных версий
- Исправление версий после ошибок
- Создание версий для специальных событий
- Когда нужно пропустить автоматическое версионирование

### Примеры ручного версионирования:

#### Patch версия (1.0.0 → 1.0.1)
**Когда использовать:**
- Исправления багов
- Небольшие улучшения
- Обновления документации
- Исправления безопасности

**Примеры:**
- Исправление критического бага в production
- Обновление зависимостей для безопасности
- Исправление опечаток в документации
- Оптимизация производительности

#### Minor версия (1.0.0 → 1.1.0)
**Когда использовать:**
- Новые функции (backward compatible)
- Значительные улучшения
- Новые возможности API
- Расширение функциональности

**Примеры:**
- Добавление новой аутентификации OAuth2
- Новые методы в API
- Поддержка новых форматов данных
- Улучшение пользовательского интерфейса
- Добавление новых конфигурационных опций

#### Major версия (1.0.0 → 2.0.0)
**Когда использовать:**
- Breaking changes (несовместимые изменения)
- Полная переработка API
- Удаление устаревших функций
- Значительные архитектурные изменения

**Примеры:**
- Изменение структуры API (несовместимые изменения)
- Удаление устаревших методов
- Изменение формата конфигурации
- Переход на новую версию языка/фреймворка
- Полная переработка архитектуры

## Практические рекомендации

### Как выбрать тип версии:

#### 🐛 **Patch (1.0.0 → 1.0.1)**
- Исправления багов
- Обновления безопасности
- Небольшие улучшения
- Исправления документации

#### ✨ **Minor (1.0.0 → 1.1.0)**
- Новые функции (не ломают существующий код)
- Значительные улучшения
- Новые возможности API
- Расширение функциональности

#### 💥 **Major (1.0.0 → 2.0.0)**
- Breaking changes
- Несовместимые изменения API
- Удаление устаревших функций
- Значительные архитектурные изменения

### Стратегия версионирования:

1. **Для ежедневной разработки**: Используйте автоматическое версионирование (patch)
2. **Для релизов**: Используйте ручное версионирование с соответствующим типом
3. **Для критических исправлений**: Используйте ручное patch версионирование
4. **Для новых функций**: Используйте ручное minor версионирование
5. **Для breaking changes**: Используйте ручное major версионирование

### Примеры workflow:

#### Ежедневная разработка:
```bash
# Обычная разработка - автоматическое patch версионирование
git add .
git commit -m "feat: add user preferences"
git push origin master
# Результат: v1.0.0 → v1.0.1 (автоматически)
```

#### Релиз новой функции:
```bash
# Создаем релиз с новой функцией
# 1. Сделать коммиты
git add .
git commit -m "feat: add OAuth2 authentication"
git push origin master
# Результат: v1.0.1 → v1.0.2 (автоматически)

# 2. Создать minor версию для релиза
# GitHub → Actions → "Tag Latest Version" → Run workflow → minor
# Результат: v1.0.2 → v1.1.0 (вручную)
```

#### Критическое исправление:
```bash
# Исправление критического бага
git add .
git commit -m "fix: resolve security vulnerability"
git push origin master
# Результат: v1.1.0 → v1.1.1 (автоматически)

# Создать patch релиз
# GitHub → Actions → "Tag Latest Version" → Run workflow → patch
# Результат: v1.1.1 → v1.1.2 (вручную)
```

#### Breaking changes:
```bash
# Подготовка к major версии
git add .
git commit -m "feat!: remove deprecated API methods"
git push origin master
# Результат: v1.1.2 → v1.1.3 (автоматически)

# Создать major версию
# GitHub → Actions → "Tag Latest Version" → Run workflow → major
# Результат: v1.1.3 → v2.0.0 (вручную)
```

## Мониторинг

### Посмотреть все версии
```bash
git tag -l
```

### Посмотреть последние версии
```bash
git tag -l | tail -5
```

### Получить текущую версию
```bash
git describe --tags --abbrev=0
```

## Настройка

Workflow использует ваш существующий скрипт `scripts/version.sh` и:
- Создает аннотированные теги версий
- Обновляет тег `latest`
- Автоматически пушит теги в репозиторий

## Отладка

Если workflow не работает:
1. Проверьте права доступа (нужен `contents: write`)
2. Убедитесь, что скрипт `version.sh` имеет права на выполнение
3. Проверьте логи в GitHub Actions 